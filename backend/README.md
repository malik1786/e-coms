# Backend Setup

To navigate here from the root in PowerShell (with spaces handled):
```powershell
cd "C:\Users\HP\Desktop\Malik\bussiness works\client2\backend"
```

## Commands
1. **Install dependencies** (required for `mongoose`):
   ```bash
   npm install
   ```
2. **Start the server**:
   ```bash
   npm run dev
   ```
