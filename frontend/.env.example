VITE_API_URL=http://localhost:5000
# Deploy note: if frontend + backend share one domain (e.g. Vercel with /api),
# you can leave VITE_API_URL empty and the app will call "/api/*" on same origin.
VITE_WHATSAPP_NUMBER=919999999999
VITE_STORE_NAME=Nova Store
